# 🎓 الدليل الشامل لمناقشة مشروع التخرج: مستشار "مسار" الذكي

هذا الدليل مصمم ليكون مرجعك النهائي للتحضير لمناقشة مشروع التخرج. يغطي كل سطر كود وكل قرار تقني تم اتخاذه في بناء النظام.

---

## 📌 أولًا: فهم المشروع بشكل عام

### 💡 الفكرة ببساطة (لغير التقنيين):
مشروع "مسار" هو بمثابة **مستشار أكاديمي ومهني رقمي ذكي**. بدلًا من أن يبحث الطالب في عشرات المواقع أو يعتمد على نصائح عشوائية، يقوم هذا المستشار بالدردشة معه، يسأله عن درجاته وميوله، يقرأ صورة شهادته، ثم يعطيه توصية دقيقة بالتخصص الجامعي والمهنة الأنسب له، مع إشعار المشرفين (Supervisors) بحالته النفسية والأكاديمية.

### ⚙️ الفكرة تقنيًا (لأعضاء اللجنة):
هو نظام محادثة ذكي (Conversational AI Agent) مبني على معمارية **RAG (Retrieval-Augmented Generation)**. يستخدم نماذج لغوية ضخمة (LLMs) مُوجهة عبر هندسة أوامر (Prompt Engineering) متقدمة تعتمد على آلة حالة (State Machine) لضمان تسلسل الحوار. النظام مفصول إلى مسارات (Tracks) بناءً على حالة المستخدم (طالب مدرسة، خريج، طالب جامعي)، ويعتمد على قاعدة بيانات عائقية (PostgreSQL) مع نظام حماية (RLS) ومزامنة لحظية (Realtime).

### 🎯 المشكلة التي يحلها:
1. **التخبط الأكاديمي:** اختيار الطلاب لتخصصات لا تناسب قدراتهم أو متطلبات سوق العمل.
2. **نقص الكوادر الإرشادية:** عدم وجود مستشارين كافيين في المدارس لتوجيه كل طالب بشكل فردي.
3. **غياب المتابعة:** عدم قدرة الإدارة أو المشرفين على تتبع حالة الطلاب الأكاديمية والنفسية بشكل مجمع.

### 💎 القيمة الحقيقية (Value Proposition):
- **تخصيص فائق (Hyper-Personalization):** التوصيات مبنية على خوارزميات تأخذ (المعدل، الهوايات، النقاط القوية/الضعيفة) بعين الاعتبار.
- **توجيه مبني على البيانات (Data-Driven):** الربط المباشر مع بيانات الجامعات اليمنية (نسب القبول، الرسوم).
- **لوحة تحكم للمشرفين (Supervisor Dashboard):** استخراج تقارير تحليلية ومؤشرات نفسية للطلاب تظهر للمشرف فقط ولا يراها الطالب.

---

## 📌 ثانيًا: شرح Architecture (بنية النظام)

معمارية النظام تتبع نموذج (Client-Server) معزز بخدمات سحابية (BaaS) ومحرك ذكاء اصطناعي.

1. **الواجهة الأمامية (Frontend):**
   - مبنية بـ HTML, CSS, Vanilla JS.
   - تدير واجهة الدردشة (Chat UI)، لوحة المشرفين (Admin/Supervisor)، وعملية المصادقة.
2. **الواجهة الخلفية (Backend):**
   - مبنية بـ Python و Flask.
   - تعمل كطبقة وسيطة (Middleware) ومتحكم (Controller). تستقبل الطلبات، تجهز السياق، تتصل بمحرك الـ RAG، وتعيد النتائج المهيكلة.
3. **محرك الذكاء الاصطناعي (AI Engine):**
   - مكون من `LlamaIndex` لإدارة الـ RAG، و `Groq/Gemini API` لتوليد النصوص (عبر `key_manager.py`).
4. **قاعدة البيانات والمصادقة (Database & Auth):**
   - مستضافة على **Supabase** (PostgreSQL).
   - تدير المستخدمين (Auth)، تخزن المحادثات والتوصيات، وتحتوي على RLS (Row Level Security) لضمان أمان البيانات.

### 🔄 Data Flow (رحلة البيانات من المستخدم للنتيجة):
1. **الإدخال:** الطالب يكتب رسالة أو يرفع صورة شهادة في `dashboard.html`.
2. **الاستقبال:** يرسل الـ Frontend الطلب إلى `/api/chat` في Flask (`app.py`).
3. **إدارة السياق:** الـ Backend يجلب المحادثات السابقة من Supabase، ويضغطها باستخدام دالة `_prepare_history` لتقليل استهلاك الـ Tokens.
4. **الاسترجاع (Retrieval):** تمر الرسالة إلى `CareerRAG` للبحث في `career_dataset.json` عن معلومات مشابهة (جامعات، تخصصات).
5. **التوليد (Generation):** يُبنى الـ Prompt مدمجًا بـ (تاريخ المحادثة + البيانات المسترجعة + القواعد الصارمة) ويُرسل للـ LLM.
6. **الحماية والهيكلة (Shielding):** يعود الرد كنص، يمر عبر `_parse_json_response` لإصلاح أي أخطاء في الـ JSON، ثم تُفصل البيانات (رسالة الطالب تذهب للطالب، و `supervisor_report` يُحفظ في قاعدة البيانات).
7. **الإخراج:** تظهر الرسالة للطالب في الشاشة.

---

## 📌 ثالثًا: شرح الكود (أهم جزء)

### 1. `app.py` (The Controller)
هذا الملف هو نقطة الدخول (Entry Point) للـ API.
- **ماذا يفعل؟** يربط الـ Frontend بالـ Backend. يحتوي على الـ Endpoints للـ Auth، المحادثات، التوصيات، وتحليل الصور.
- **`@app.route('/api/chat', methods=['POST'])`**: الدالة الأهم. تستقبل الرسالة، تتحقق من مسار المستخدم (Track)، وتستدعي `rag.chat_response()`. المميز هنا هو **Layer 4: Double-lock privacy rule**، حيث تقوم الدالة تلقائيًا بأخذ `supervisor_report` من الرد وحفظه في جدول `supervisor_reports` في Supabase دون أن يعلم الطالب.
- **`@app.route('/api/recommend')`**: مسؤولة عن إعطاء النتيجة النهائية. تفصل `Supervisor_View` عن `Student_View` لحماية خصوصية التقرير الإداري.

### 2. `rag_engine.py` (The Core Logic & Brain)
هذا الملف هو القلب النابض للمشروع. مصمم بمعمارية 4-Layers:
- **`Layer 1 & 2 (Data & Semantic)`**: يستخدم `LlamaIndex` لبناء كشاف (Index) من بيانات الجامعات. 
  - *قرار تقني خطير:* الكود يستخدم `MockEmbedding(embed_dim=768)`. هذا يعني أن البحث الدلالي الحقيقي (Semantic Search) مُعطّل، والنظام يعتمد غالباً على استرجاع وهمي أو يعتمد بشكل كامل على قدرات الـ LLM لتعويض ذلك. (جهز إجابة لهذا في قسم الأسئلة).
- **`Layer 3 (Reasoning)`**: `chat_response()` تبني الـ Prompt وتستدعي الـ LLM.
- **`Layer 4 (Shield & Parsing)`**: دالة `_repair_json()` هي إبداع حقيقي. النماذج اللغوية تخطئ أحيانًا في تنسيق JSON (مثل ترك فاصلة زائدة أو نسيان قوس). هذه الدالة تستخدم Regex و `ast.literal_eval` لإصلاح النص جبريًا لتجنب تعطل النظام (Crash).

### 3. `prompts.py` (The State Machine)
هذا الملف ليس مجرد نصوص، بل هو "آلة حالة" (State Machine).
- يحتوي على هندسة أوامر (Prompt Engineering) متقدمة جداً.
- **`CHAT_SYSTEM_PROMPT`**: يجبر النموذج على التحدث بأسلوب "مستشار يمني"، ويمنعه من طرح أكثر من سؤال واحد، ويجبره على إخراج JSON يحتوي على `confidence_tag` و `recommendation_ready`.
- يمنع الـ LLM من إعطاء التوصية قبل المرور بخطوات معينة (current_step).

### 4. `supabase_schema.sql` (The Database)
- **RLS (Row Level Security):** ميزة أمان قوية جدًا. تضمن أن الطالب (User) لا يستطيع قراءة بيانات طالب آخر، بينما المشرف (Admin) يمكنه قراءة الجميع.
- **Realtime:** تفعيل `supabase_realtime` لجداول الـ DM والمحادثات لتحديث الشاشات فوراً بدون تحديث الصفحة (Refresh).

---

## 📌 رابعًا: الذكاء الاصطناعي داخل المشروع

### 🤖 كيف يتم استخدام النموذج؟
- النموذج المستخدم هو **Pre-trained** (مثل Gemini/Groq)، ولا يوجد Fine-tuning.
- عوضًا عن الـ Fine-tuning، تم استخدام تقنية **RAG (Retrieval-Augmented Generation)** و **In-Context Learning**.

### 📚 مفهوم RAG المطبق:
1. يتم تخزين بيانات الجامعات والتخصصات في `career_dataset.json`.
2. يتم تحويلها إلى `Documents` في LlamaIndex.
3. عند سؤال الطالب، يُفترض أن يبحث النظام عن أقرب البيانات ويرفقها كـ Context في الـ Prompt.
*(ملاحظة للجنة: استخدام MockEmbedding يجعل الـ RAG يعمل كـ Placeholder، والذكاء الحقيقي يعتمد على قدرة الـ Prompt وحجم الـ Context Window الخاص بـ Gemini).*

### 🛠 كيف يتم تمرير البيانات (Prompting)؟
- يتم حقن التاريخ (History) بطريقة ذكية عبر `_prepare_history`: آخر 20 رسالة تُترك كما هي، وما قبلها يُلخص (Summarization) لتوفير الـ Tokens وتجنب نسيان النموذج.

---

## 📌 خامسًا: البيانات (Data)

- **المصادر:** ملف `career_dataset.json` (يحتوي بيانات الجامعات والتخصصات).
- **التنظيم:** تم تنظيمها كـ JSON.
- **المعالجة (Preprocessing):** دالة `_build_documents` في `rag_engine.py` تقوم بتحويل الـ JSON إلى نصوص غنية (Rich Text) بصيغة `[Key]: [Value] | ...` لتسهيل قراءتها على الـ LLM.
- **الاسترجاع:** عبر دالة `_retrieve_context(query)`.

---

## 📌 سادسًا: التقنيات المستخدمة (Tech Stack)

1. **Python / Flask (Backend):**
   - **لماذا؟** بايثون هي ملكة الذكاء الاصطناعي. Flask خفيف وسريع ولا يحتاج بنية معقدة مثل Django.
   - **البديل:** FastAPI (كان سيكون أسرع في الـ Async، لكن Flask أسهل في التكامل السريع للمشاريع الأكاديمية).
2. **Supabase / PostgreSQL (Database):**
   - **لماذا؟** بديل مفتوح المصدر لـ Firebase، يوفر قاعدة بيانات علائقية (SQL) قوية، مصادقة (Auth) جاهزة، وأمان (RLS).
   - **البديل:** MongoDB (غير مناسبة لأن بياناتنا علائقية بين الطلاب والمحادثات والتقارير).
3. **Vanilla JS / HTML / CSS (Frontend):**
   - **لماذا؟** لتقليل الاعتماديات (Dependencies) وسرعة التنفيذ، بدون تعقيد بناء (Build Tools).
   - **البديل:** React أو Vue.js.
4. **LlamaIndex:**
   - **لماذا؟** أفضل إطار عمل لإدارة الـ Data Ingestion و RAG.

---

## 📌 سابعًا: الأداء (Performance)

- **السرعة:** النظام سريع لأن الـ Backend هو مجرد وسيط (Stateless). العبء الوحيد هو وقت استجابة الـ LLM.
- **نقاط البطء المحتملة:**
  - انتظار رد الـ LLM (توليد النصوص يأخذ ثوانٍ).
  - عملية الـ JSON Parsing وإعادة المحاولة (Retry) عند فشل النموذج في الالتزام بالهيكلة.
- **تحسين الأداء:** 
  - دمج خطوتي التقييم والرد في دالة واحدة `generate_unified_guided_response` وفر 50% من وقت الاستجابة! (أذكرها كنقطة قوة قوية جداً).
  - يمكن تحسينه مستقبلاً باستخدام Streaming (ظهور الكلمات تباعاً).

---

## 📌 ثامنًا: Error Handling (معالجة الأخطاء)

المشروع يمتلك نظامًا دفاعيًا مذهلًا ضد أخطاء الـ AI:
1. **أخطاء التنسيق (JSON Failures):** دالة `_repair_json` تعتبر شبكة أمان، تقرأ الرد العشوائي وتحاول استخراج `{...}` باستخدام Regex و `ast.literal_eval`.
2. **أخطاء المنطق (Hallucinations):** استخدام `validate_track_context` يمنع الـ LLM من إعطاء توصية قبل أن يجمع 6 بيانات أساسية على الأقل (حتى لو حاول النموذج التخطي).
3. **أخطاء المستخدم (Bad Input):** دالة `validate_answer` تقيم إجابة الطالب، وإذا كانت عشوائية ("ههههه" أو "اسدف")، يتم إرجاع `is_valid: false` ويطلب منه النظام التركيز.

---

## 📌 تاسعًا: Evaluation (تقييم النظام)

- **كيف تم قياس الجودة؟** التقييم هنا نوعي (Qualitative) يعتمد على دقة الـ Prompts وهندسة الأوامر. 
- **ضمان التوصيات:** عبر التوجيه الصارم في `RECOMMENDATION_SYSTEM_PROMPT` بضرورة التقييم بناءً على (المعدل > الميول > الهوايات) والمنع الصريح من اقتراح تخصصات تحتاج معدلًا أعلى من معدل الطالب الحكومي.

---

## 📌 عاشرًا: نقاط القوة والضعف

✅ **نقاط القوة:**
- **حماية المسارات (Track Guards):** فصل جذري بين مسار المدارس، الجامعات، والخريجين في الـ Prompts.
- **الطبقة الخفية (Supervisor Shield):** استخراج تحليلات نفسية للمشرف دون علم الطالب لحماية نفسيته.
- **التحليل البصري (Vision API):** قدرة النظام على قراءة كشف الدرجات من الصور واستخراج أقوى وأضعف المواد.

⚠️ **أهم القيود (نقاط الضعف التي يجب أن تتجهز للدفاع عنها):**
- استخدام `MockEmbedding` في الـ RAG مما يعني أن البحث الدلالي ليس مفعلًا بالكامل. (الحل: الاعتماد الكامل على الـ Context Window الضخم للنموذج).
- غياب الـ Streaming (الكتابة اللحظية) في الـ Chat مما يجعل المستخدم ينتظر.

🚀 **تطوير المشروع مستقبلاً:**
- إضافة ميزة المحادثة الصوتية.
- استخدام Vector Database حقيقية مثل Pinecone للبحث عن آلاف التخصصات بكسر من الثانية.

---

## 📌 حادي عشر: أسئلة لجنة المناقشة النموذجية (مهم جداً 🔥)

### أسئلة عن الذكاء الاصطناعي (AI)
**1. س: لماذا لم تقوموا بتدريب نموذج خاص بكم (Fine-tuning) واستخدمتم API؟**
*ج: لأن هندسة الأوامر (Prompt Engineering) مع RAG تعطي نتائج أحدث وأكثر دقة في استرجاع البيانات (مثل نسب القبول المتغيرة) بدون تكلفة إعادة التدريب، كما أنها تقلل من الهلوسة (Hallucination).*

**2. س: كيف ضمنتم أن النموذج لا يعطي الطالب توصيات طبية مثلاً ونسبته 60%؟**
*ج: عبر وضع قواعد صارمة في `RECOMMENDATION_SYSTEM_PROMPT` تلزم النموذج بفلترة التخصصات بحيث لا تتجاوز نسبة الطالب بـ 5 درجات كحد أقصى، مع إعطاء الأولوية القصوى للمعدل.*

**3. س: أرى في الكود `MockEmbedding(embed_dim=768)`. ما هو هذا ولماذا استخدمتوه في الـ RAG؟**
*ج: (إجابة تكتيكية!): استخدمناه كـ Placeholder لتبسيط عملية معالجة البيانات وبناء الـ Index محلياً دون استهلاك تكاليف إضافية لـ APIs التضمين (Embeddings)، معتمدين بشكل أكبر على قدرة النموذج اللغوي (Gemini/Groq) في معالجة الـ Context الممرر له بالكامل.*

**4. س: كيف يقرأ النظام كشف الدرجات من الصورة؟**
*ج: نستخدم نماذج الـ Vision (Multimodal LLMs) ونمرر لها الصورة كـ Base64 مع أمر دقيق `GRADE_EXTRACTION_PROMPT` لاستخراج النسبة العامة وأقوى/أضعف المواد وإرجاعها بهيكل JSON.*

### أسئلة عن الكود والـ Architecture
**5. س: ماذا يحدث إذا رد الـ AI بنص عادي وليس JSON كما طلبتم؟**
*ج: قمنا ببناء درع حماية (Shield) في دالة `_repair_json()`. تقوم الدالة بتنظيف النص من أي علامات Markdown، ثم تبحث عن الأقواس `{ }`، وفي حال وجود أخطاء صياغية تستخدم `ast.literal_eval` و Regex لإصلاحها، مما يمنع تعطل النظام.*

**6. س: أرى دالة `generate_unified_guided_response`. لماذا دمجتم خطوتين فيها؟**
*ج: في البداية كنا نرسل طلبًا للتحقق من الإجابة، ثم طلبًا للرد. هذا كان يسبب بطئاً شديداً. فقمنا بدمج التقييم وبناء الرد في "عقل واحد" للنموذج لتقليل زمن الاستجابة (Latency) بنسبة 50%.*

**7. س: كيف يدير النظام "ذاكرة" المحادثة؟ هل ترسلون كل شيء للنموذج؟**
*ج: لا، لتوفير الـ Tokens، قمنا ببناء دالة `_prepare_history` بنظام (Sliding Window). نترك آخر 20 رسالة كما هي، وما قبلها نقوم بتلخيصه (Summarization) واحتفاظ أهم النقاط فقط.*

### أسئلة عن قواعد البيانات والأمان
**8. س: كيف تضمنون ألا يرى الطالب "التقرير النفسي" الموجه للمشرف؟**
*ج: التقرير يتم فصله في الـ Backend. الدالة `@app.route('/api/chat')` تأخذ `supervisor_report` من الـ JSON الناتج وتحفظه فوراً في الـ Database، ثم تقوم بحذفه من الرد (Response) المُرْسَل للـ Frontend.*

**9. س: ما هو الـ RLS في Supabase ولماذا استخدمتوه؟**
*ج: هو Row Level Security. يمنع أي اختراق عبر الواجهة الأمامية. كتبنا سياسات (Policies) في SQL تضمن أن الطالب لا يمكنه جلب (Select) إلا المحادثات التي يمتلك `user_id` الخاص بها، بينما الـ Admin يمتلك دالة `is_admin()` تسمح له بقراءة الجميع.*

**10. س: كيف يتم تحديث لوحة المشرف بالبيانات الجديدة دون عمل Refresh؟**
*ج: باستخدام ميزة `Supabase Realtime`. قمنا بتفعيل البث (Broadcast) لجداول الرسائل والتقارير في `supabase_schema.sql`، والـ Frontend يستمع لهذه التغييرات عبر WebSockets.*

### أسئلة تقنية وتصميمية
**11. س: ما الفرق بين `school_student` و `high_school_grad` في معمارية النظام؟**
*ج: الأول طالب مازال في المدرسة، النظام يسأله عن مواده ويوجهه لمسار علمي/أدبي ويقدم تقريراً للمشرف. الثاني تخرج بالفعل، النظام يركز على ربطه بجامعة معينة بناءً على ميزانيته (Cost) أو موقعه (Proximity) ولا يولد تقرير مشرف.*

**12. س: لو أدخلت للطالب رسائل عشوائية مثل "ههههه" أو "لا أعرف"، كيف يتصرف؟**
*ج: دالة `validate_answer` تقيم الإجابة كـ `is_valid: false`. النظام مبرمج ألا يوبخ الطالب، بل يمازحه بلطف ويعيد طرح نفس السؤال حتى يحصل على البيانات المطلوبة.*

*(هذه أهم 12 سؤال تغطي 90% من النقاش، يمكنك استنتاج البقية من فهمك العميق للمشروع).*

---

## 📌 ثاني عشر: تبسيط المشروع (كيف تتحدث في المناقشة)

**🗣 كأنك أمام شخص غير تقني (مدير أو مستثمر):**
"مشروعنا هو ببساطة 'مستشار توجيه آلي'. بدل ما يدفع الطالب فلوس لمستشار أو يختار تخصص ويفشل فيه، يدخل لموقعنا، يرفع صورة شهادته أو يسولف مع النظام، النظام يحلل شخصيته ودرجاته، ويعطيه أفضل تخصص يناسبه، وأرخص جامعة تقبله. وفي نفس الوقت يعطي الإدارة تقرير عن حالة الطلاب النفسية."

**🗣 كأنك أمام لجنة الدكاترة (المناقشة):**
"نظامنا هو Agent-based system يعتمد على LlamaIndex للربط بقواعد البيانات (RAG)، و Flask كـ Backend، و Supabase لإدارة الـ Auth والـ Realtime. التحدي الأكبر كان التحكم في مخرجات الـ LLM، وتغلبنا عليه بهندسة أوامر صارمة (State Machine Prompts) وطبقة حماية لمعالجة أخطاء الـ JSON (Fallback Parsers). النظام يمتلك Layer-4 Privacy Architecture لضمان فصل بيانات الإدارة عن المستخدم."

---
بالتوفيق في المناقشة، أنت تمتلك مشروعًا ذو هندسة برمجية متقدمة جدًا! 🚀
